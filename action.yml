name: "AutoChangelog"
description: "Generate changelog body.txt for GitHub releases"

author: "XPGAMESLLC"

inputs:
  organization:
    description: "GitHub organization"
    required: true
  repo_name:
    description: "GitHub repository name"
    required: true
  file_name:
    description: "Output file name"
    required: false
    default: "body.txt"
  auth_token:
    description: "GitHub token used for API access"
    required: true
  python_version:
    description: "Python version used by the action"
    required: false
    default: "3.12"
  ai_summary:
    description: "Enable ChatGPT summary section"
    required: false
    default: "false"
  ai_model:
    description: "ChatGPT model for summaries"
    required: false
    default: "gpt-4o-mini"
  ai_max_items:
    description: "Max items per category sent to AI"
    required: false
    default: "120"
  ai_api_key:
    description: "API key used for ChatGPT summarization"
    required: false
    default: ""
  ai_base_url:
    description: "Optional base URL for OpenAI-compatible endpoint"
    required: false
    default: "https://api.openai.com/v1"

runs:
  using: "composite"
  steps:
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python_version }}

    - name: Install AutoChangelog package
      shell: bash
      run: python -m pip install --upgrade pip && python -m pip install "${{ github.action_path }}"

    - name: Generate changelog
      shell: bash
      env:
        AUTH_TOKEN: ${{ inputs.auth_token }}
        AI_API_KEY: ${{ inputs.ai_api_key }}
        AI_BASE_URL: ${{ inputs.ai_base_url }}
      run: |
        EXTRA_ARGS=""
        if [ "${{ inputs.ai_summary }}" = "true" ]; then
          EXTRA_ARGS="--ai-summary --ai-model ${{ inputs.ai_model }} --ai-max-items ${{ inputs.ai_max_items }}"
        fi
        python -m releasenotes.generator "${{ inputs.organization }}" "${{ inputs.repo_name }}" --file_name "${{ inputs.file_name }}" $EXTRA_ARGS
